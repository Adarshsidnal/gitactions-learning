{
  "name": "github-actions-learning-app",
  "version": "1.0.0",
  "private": true,
  "description": "A small full-stack app for learning GitHub Actions.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "lint": "node --check server.js && node --check public/app.js",
    "build": "node scripts/build.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}